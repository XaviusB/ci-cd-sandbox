---
- name: Restart SSH service
  ansible.builtin.systemd:
    name: ssh
    state: restarted
    daemon_reload: true
  become: true

- name: Reload SSH service
  ansible.builtin.systemd:
    name: ssh
    state: reloaded
  become: true
